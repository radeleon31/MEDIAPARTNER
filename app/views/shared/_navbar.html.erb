<nav class="nav-fixed-black">
      <div class="nav-center">
        <!-- nav header -->
        <% if user_signed_in? %>
          <div class="nav-header">
            <div class="navbar-brand">
              <text fill="#ffff" stroke="#ffff" stroke-width="0.5" x="62.5" y="17.5" id="svg_1" font-size="24" font-family="Georgia" text-anchor="middle" xml:space="preserve" font-weight="bold" font-style="normal" calls><strong>Media</strong>Partner</text>
            </div>

            <button class="nav-toggle">
              <i class="fas fa-bars"></i>
            </button>
          </div>
        <% else %>
        <div class="nav-header">
            <div class="navbar-brand">
              <%= image_tag "logo.png" %>
            </div>

            <button class="nav-toggle">
              <i class="fas fa-bars"></i>
            </button>
          </div>
        <% end %>
        <!-- links -->
        <% if user_signed_in? %>
          <ul class="links">
            <li>
              <%= link_to 'Overview', overview_path, class:"btn btn-flat subrayado"%>
            <li>
            <li>
              <%= link_to 'Publisher', publishings_path, class:"btn btn-flat subrayado"%>
            <li>

            <li>
              <li class="dropdown">
                <a class="btn btn-flat subrayado" data-toggle="dropdown">
                  Monetizer <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li> <%= link_to "Insight",  last_day_channel_path(1), class:"dropdown-item" %> </a></li>
                  <li> <%= link_to "Performance",  channels_path(1), class:"dropdown-item" %> </a></li>
                </ul>
            <li class="nav-item dropdown">
              <img class="avatar dropdown-toggle" id="navbarDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" src="https://kitt.lewagon.com/placeholder/users/ssaunier" />
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
              <a ><%= link_to 'account', mypublishings_path, class:"nav-link"%></a>
              <a> <%= link_to 'Logout', destroy_user_session_path, method: :delete, class:"nav-link"%></a>
              </div>
              </li>
            </li>


          </ul>
        <% else %>
          <ul class="links">
            <li>
              <%= link_to 'Login', new_user_session_path, class:"btn btn-flat"%>
            </li>
            <li>
              <%= link_to 'Sign Up', new_user_registration_path, class:"btn btn-flat" %>
            </li>
          </ul>
        <% end %>
      </div>
    </nav>


